export interface AddressDTO {
  addressId: string;
  street: string;
  state: string;
  city: string;
  country: string;
  isDefault: boolean;
}
export interface AddressAddDTO {
  street: string;
  state: string;
  city: string;
  country: string;
  isDefault: boolean;
}
